import 'package:flutter/material.dart';

class Messsage extends StatelessWidget {
  const Messsage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Message Page"),);
  }
}
